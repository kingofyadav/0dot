// Shared fallback description for generateMetadata across the root layout
// and every page with a real subject (profile/business/community) that
// falls back to it when that subject hasn't written their own bio/tagline/
// description — one string kept in sync rather than copies drifting.
export const SITE_DESCRIPTION =
  "Claim your permanent home on the internet — a profile, a link hub, and a feed, all under one identity.";
